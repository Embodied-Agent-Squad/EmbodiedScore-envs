[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "embodiedscore-envs"
version = "0.2.0"
description = "Every habitat benchmark of EmbodiedScore on one frozen habitat-sim 0.3.3, behind Gymnasium 1.3"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
    "gymnasium>=1.3",
    "numpy>=1.24,<2",
    "numpy-quaternion",
    "scipy",
    "fastdtw",
    "pillow",
]
# habitat-sim is NOT a pip dependency: install it from
# https://github.com/Embodied-Agent-Squad/EmbodiedScore-habitat (see its BUILD.md).

[project.optional-dependencies]
test = ["pytest"]

[tool.setuptools.packages.find]
include = ["embodiedscore_envs*"]
